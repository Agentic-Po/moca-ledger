# Data bank

Generated by `catalog.py` on every crawl — **do not edit by hand**.
Rows, bytes and coverage are measured off the files, never asserted.

**Data bank: 186,488 rows · 44.9 MB across 3 datasets**

## ledger

| Dataset | Path | Rows | Size | Coverage | Cadence |
|---|---|---:|---:|---|---|
| `ledger` | `data/` | 186,386 | 44.9 MB | 2026-07-06 → 2026-08-30 | every crawl (10-minute cron) |

**`ledger`** — block, ts (unix), tx, li (log index), from, to, value (raw wei string)

- provenance: eth_getLogs over the MOCA contract on Base, adaptive window, resumable via state.json; timestamps derived from Base's fixed 2s block time (verified exact over 2M blocks)
- not included: MOCA Transfer events only — no other token, no ETH, no internal calls, and no USD pricing (this repo never prices a row)

## oracle

| Dataset | Path | Rows | Size | Coverage | Cadence |
|---|---|---:|---:|---|---|
| `labels` | `labels/` | 101 | 5.4 KB | 2026-08-22 → 2026-08-22 | hand-maintained; changes ride with a PR |

**`labels`** — public_addresses.json: infrastructure[], token_contracts{}, event_topics{}; allowlist.json; calendar.json: campaign windows; labels-lite.json

- provenance: public infrastructure identified off-chain (treasury, reward source, cognition sink, AMM pools) plus campaign windows
- not included: no account-level mapping of any kind; the excluded-address list is a salted hash set and exit_watch.json is private (see below)

## derived

| Dataset | Path | Rows | Size | Coverage | Cadence |
|---|---|---:|---:|---|---|
| `heartbeat` | `heartbeat.json` | 1 | 508 B | 2026-08-30 → 2026-08-30 | rewritten every crawl (10-minute cron) |

**`heartbeat`** — run_ts, crawl_ok, detect_ok, notify_ok, rows_total, ledger_last, lag_blocks, mindset_age_h, open_findings, fires_last_24h_total

- provenance: written by the crawl-detect-notify workflow at the end of each run
- not included: counts and health flags only — no finding detail, no addresses, no thresholds

## private (not published)

Listed so the absence is deliberate and visible. No schema, no size,
no coverage: a findings file's size is a count of open findings.

| Dataset | Kind | Note |
|---|---|---|
| `alerts/state.json` | derived | private — see companion doc |
| `detect/balances.json` | derived | private — see companion doc |
| `labels/exit_watch.json` | oracle | private — see companion doc |
| `alerts/msglog/` | archive | private — see companion doc |

